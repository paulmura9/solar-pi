.env
venv/
__pycache__/
*.pyc
*.pyo
.vscode/
.DS_Store
gateway_old.py

models/*.tflite
!models/.gitkeep

/var/lib/solar-tracker/
buffer.db

# Captured training images (large, regenerated on the Pi). The manifest.csv stays
# tracked so the dataset's contents are versioned without the binary payload.
dataset/raw/

.claude/
.mcp.json
CLAUDE.md
SKILL.md
AGENTS.md